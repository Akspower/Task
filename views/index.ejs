<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Task Manager</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white">
    <div class="main w-full min-h-screen flex flex-col items-center py-10">
        <!-- Form Section -->
        <div class="form bg-gradient-to-r from-purple-500 to-blue-500 p-8 rounded-lg shadow-lg w-full max-w-lg mb-10 transform transition-transform hover:scale-105 duration-300">
            <h2 class="text-3xl font-bold mb-6 text-center text-white animate-bounce">Create a New Task</h2>
            <form action="/create" method="post" class="space-y-6">
                <div>
                    <label for="title" class="block text-lg font-medium mb-2 text-white">Title</label>
                    <input type="text" id="title" placeholder="Title" class="w-full p-3 rounded bg-zinc-700 border border-zinc-600 text-white focus:outline-none focus:ring-2 focus:ring-pink-500" name="title">
                </div>
                <div>
                    <label for="details" class="block text-lg font-medium mb-2 text-white">Task Details</label>
                    <textarea id="details" placeholder="Write your Task Details" class="w-full p-3 rounded bg-zinc-700 border border-zinc-600 text-white focus:outline-none focus:ring-2 focus:ring-pink-500" rows="4" name="details"></textarea>
                </div>
                <div>
                    <input type="submit" value="Create Task" class="w-full p-3 rounded bg-pink-600 hover:bg-pink-700 text-white font-bold cursor-pointer transform hover:scale-105 transition-transform duration-300">
                </div>
            </form>
        </div>

        <!-- Tasks Section -->
        <div class="tasks flex flex-wrap justify-center gap-6 w-full px-5">
            <% if(files.length > 0) { %>
                <% files.forEach(function(val) { %>
                    <div class="task w-72 px-5 py-6 rounded-lg bg-gradient-to-r from-indigo-600 to-purple-600 shadow-lg hover:shadow-xl transform hover:scale-105 transition-transform duration-300">
                        <h1 class="text-2xl font-bold tracking-tight mb-3 text-white"><%= val %></h1>
                        <p class="text-zinc-300 mb-3">This is a brief description of the task. Keep it concise and informative.</p>
                        <a class="text-blue-300 hover:text-white hover:underline" href="/file/<%= val %>">Read more.</a>
                    </div>
                <% }) %>
            <% } else { %>
                <h3 class="text-zinc-600 text-center mt-10 animate-pulse">NO TASK YET...</h3>
            <% } %>
        </div>
    </div>

    <script src="/javascripts/script.js"></script>
</body>
</html>
